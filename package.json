{
  "private": true,
  "devDependencies": {
    "@prettier/plugin-xml": "^0.13.1",
    "eslint": "^7.17.0",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "prettier": "^2.2.1"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{xml,css,md,yml,yaml}": "prettier --write"
  },
  "scripts": {
    "test": "eslint --cache .",
    "prepare": "husky install"
  }
}
